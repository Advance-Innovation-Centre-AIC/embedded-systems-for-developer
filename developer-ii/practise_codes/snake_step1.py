# โครงเว้น 30% — ส่วนที่คุณต้องเติมเองดูที่ขั้นล่าง / เฉลย: starter/snake_step1.py / ใบ้: ในใบงาน session
# (ขั้นนี้เน้นทดลองแก้ค่าที่มีอยู่ให้เป็นของคุณ ยังไม่ต้องเขียน logic ใหม่)
# snake_step1.py — สร้าง Snake #1 (คาบ 4): วาดสนาม + ตัวงู + คะแนน (ยังไม่ขยับ)
# เป้าหมาย: หน้าจอใกล้เคียง snake_game.png (ภาพเกมจริง) แบบภาพนิ่ง
#
# เรื่องย่อของคาบนี้: เราจะวาดงูลงบนกริด เหมือนวางหมากลงกระดาน
#   จอแบ่งเป็นช่องสี่เหลี่ยมเท่า ๆ กัน (กริด) แล้วงูคือกล่องเรียงต่อกันเป็นช่อง ๆ
#   คาบนี้ยังไม่ให้งูเดิน เราขอแค่ให้เห็นภาพนิ่งที่หน้าตาเหมือนเกมจริงก่อน
import bentogame as game

CELL_PX = 26                                # 1 ช่องกริด = 26 พิกเซล
GRID_COLS = game.WIDTH // CELL_PX           # หน้าจอกว้างกี่ช่อง
GRID_ROWS = game.HEIGHT // CELL_PX          # หน้าจอสูงกี่ช่อง

game.title("SNAKE")                          # หน้าเริ่ม: Start=เล่น Back=ออก (ทำ start ให้ในตัว)

# งู = list ของช่อง [คอลัมน์, แถว] โดยหัวงูอยู่ช่องแรกของ list เสมอ จำกฎนี้ไว้ให้ดีนะ
snake_body = [[6, 8], [5, 8], [4, 8]]

# แปลงช่องกริดของแต่ละปล้องให้เป็นกล่องสีเขียวบนจอ (ช่อง x 26 พิกเซล = ตำแหน่งจริง)
body_squares = [game.Box(col * CELL_PX, row * CELL_PX, CELL_PX - 2, CELL_PX - 2, game.GB_DARK)
                for col, row in snake_body]
body_squares[0].set_color(game.GB_LIGHT)    # หัวงูสีอ่อนกว่า ให้เห็นชัดว่าหัวอยู่ไหน

food_square = game.Box(7 * CELL_PX, 4 * CELL_PX, CELL_PX - 2, CELL_PX - 2, game.GB_LIGHTEST)
score = 0
score_text = game.Text("Score: 0", 10, 8, game.WHITE)

# ----- เติมส่วนนี้เอง (งานของคุณ): ทดลองแก้ภาพนิ่งให้เป็นของคุณ (คาบนี้เน้นเล่นกับโค้ดที่มีอยู่ ยังไม่เพิ่ม logic) -----
#    1) เปลี่ยนตำแหน่งงูเริ่มต้น โดยแก้ค่าช่อง [คอลัมน์, แถว] ใน snake_body ด้านบน แล้วรันดูว่างูย้ายไหม
#    2) เปลี่ยนสีหัวงู: เรียก .set_color(...) บน body_squares[0] โดยลองใส่ค่าสีตัวอื่น เช่น game.GB_LIGHTEST / game.WHITE
#    3) ลองย้ายอาหาร: เปลี่ยนช่องของ food_square (เลข 7 กับ 4 ที่คูณ CELL_PX) แล้วดูว่าจุดอาหารขยับตามไหม
#    (ทุกข้อแก้ค่าที่มีอยู่แล้วได้เลย ไม่ต้องเพิ่มบรรทัดใหม่ ถ้าติดดูที่ starter/snake_step1.py)

# ภาพยังนิ่ง คาบหน้าเราจะเติม logic เดินใน update() (Back = ออก / Start = เริ่มใหม่ จัดการให้โดย game.run())
def update():
    pass                                    # ทุกเฟรมยังไม่ต้องทำอะไร ภาพจึงนิ่ง

game.run(update, fps=9)
