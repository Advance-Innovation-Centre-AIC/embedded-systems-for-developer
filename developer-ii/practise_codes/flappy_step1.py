# โครงเว้นบางส่วน — เติมช่องที่เป็นงานของคุณให้ครบ แล้วเกมจะทำงาน
# เฉลย: starter/flappy_step1.py / ใบ้: ในใบงาน session
# -----------------------------------------------------------------------------
# flappy_step1.py — Flappy #1: นกลอยนิ่งบนท้องฟ้า + คะแนน (ยังไม่ตก)
# -----------------------------------------------------------------------------
# เรื่องราวของคาบนี้:
#   ก่อนที่นกจะบินได้ เราต้องวาดนกขึ้นจอให้ได้ก่อน. คาบนี้เราแค่วางนกนิ่งๆ
#   ไว้กลางฟ้า แล้วโชว์คะแนน 0 เพื่อให้เห็นว่าเราทำเกมขึ้นจอเองได้จริง.
#   ยังไม่มีฟิสิกส์หรือท่อ คาบหน้าค่อยทำให้มันตกแล้วกระพือ.
#
# ส่วนที่คุณเขียนเองคาบนี้: เลือกความสูงเริ่มต้นของนก แล้ววางกล่องนกไว้ตรงนั้น.
# ส่วน core ที่เราเรียกใช้: game.start(), game.Box(), game.Text(), game.run()
#   (bentogame.py:154,178,227,370)
# C step ที่ตรงกับ Python step บนจอ: c_track/flappy_step1.c (นกนิ่ง + คะแนน 0)
# อ้างอิง: reference/flappy_full.py:12-17
import bentogame as game

game.title("FLAPPY")                          # หน้าเริ่ม: Start=เล่น Back=ออก (ทำ start ให้ในตัว)

# ----- เติมส่วนนี้เอง (งานของคุณ): เลือกความสูงเริ่มต้นของนก -----
# สิ่งที่ต้องทำ:
#   1) นึกถึงความสูง (พิกเซลนับจากขอบบนจอ) ที่อยากให้นกลอย เอาราวๆ กลางฟ้า
#   2) ตั้งค่าตัวแปร bird_y ให้เท่ากับเลขนั้น (ใช้ค่าทศนิยม เพราะคาบหน้าจะใช้ฟิสิกส์)
#   3) ลองรันดู ถ้านกสูงหรือต่ำไป ค่อยปรับเลขเอง (ถ้าติดจริงเปิด starter/ ดูได้)
bird_y = 0.0   # แก้ตัวเลขนี้ให้นกไปลอยกลางฟ้า
# ---------------------------------------------------------------

bird = game.Box(150, bird_y, 34, 34, game.GB_LIGHTEST)   # ตัวนก = กล่องสว่างสุด
score = 0
score_label = game.Text("0", 384, 16, game.WHITE)

def on_each_frame():
    # ทุกเฟรม เราวาดนกไว้ที่ความสูง bird_y ตอนนี้ยังไม่ขยับ เป็นภาพนิ่ง
    bird.move_to(bird.x, bird_y)
    if game.keys().start:        # กด Start เพื่อออกจากเกม (เหมือนทุกเกม)
        return False
    # ขั้นนี้ยังไม่มีส่วนที่คุณต้องเติมใน on_each_frame แค่รันดูผลงานวาด
    # คาบหน้า เราจะเพิ่มแรงโน้มถ่วงให้นกตก แล้วกด A เพื่อกระพือขึ้น

game.run(on_each_frame, fps=50)                 # bentogame.py:370 — ลูปเกม 50 fps
