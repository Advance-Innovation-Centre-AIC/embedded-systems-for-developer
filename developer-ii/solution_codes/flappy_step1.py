# flappy_step1.py — สร้าง Flappy ขั้นที่ 1: นกลอยนิ่งบนท้องฟ้า + คะแนน (ยังไม่ตก)
# -----------------------------------------------------------------------------
# เรื่องราวของคาบนี้:
#   ก่อนที่นกจะบินได้ เราต้องวาดนกขึ้นจอให้ได้ก่อน. คาบนี้เราแค่วางนกนิ่งๆ
#   ไว้กลางฟ้า แล้วโชว์คะแนน 0 เพื่อให้เห็นว่าเราพาเกมขึ้นจอได้ด้วยมือเราเอง.
#   ยังไม่มีฟิสิกส์หรือท่อ คาบหน้าเราค่อยทำให้มันตกแล้วกระพือกัน.
#
# ส่วนที่คุณเขียนเองคาบนี้: เลือกความสูงเริ่มต้นของนก แล้ววางกล่องนกไว้ตรงนั้น.
# ส่วนที่ core เรียกให้: game.start(), game.Box(), game.Text(), game.run()
#   (bentogame.py:154,178,227,370)
# C step เทียบกับ Python step บนจอ: c_track/flappy_step1.c (นกนิ่ง + คะแนน 0)
# อ้างอิง: reference/flappy_full.py:12-17
import bentogame as game

game.title("FLAPPY")                          # หน้าเริ่ม: Start=เล่น Back=ออก (ทำ start ให้ในตัว)

# ----- เติมส่วนนี้เอง: ตำแหน่งเริ่มต้นของนก -----
bird_y = 180.0                                  # ความสูงของนก (พิกเซลจากขอบบน)
bird = game.Box(150, bird_y, 34, 34, game.GB_LIGHTEST)   # ตัวนก = กล่องสว่างสุด
score = 0
score_label = game.Text("0", 384, 16, game.WHITE)

def on_each_frame():
    # ทุกเฟรม วาดนกไว้ที่ความสูง bird_y (ยังไม่ขยับ — เป็นภาพนิ่ง)
    bird.move_to(bird.x, bird_y)
    if game.keys().start:        # กด Start เพื่อออกจากเกม (เหมือนทุกเกม)
        return False
    # คาบหน้า: เพิ่มแรงโน้มถ่วงให้นกตก แล้วกด A เพื่อ "กระพือ" ขึ้น

game.run(on_each_frame, fps=50)                 # bentogame.py:353 — ลูปเกม 50 fps
