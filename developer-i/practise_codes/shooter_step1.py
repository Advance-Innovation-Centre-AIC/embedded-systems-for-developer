# โครงเว้น 30% — เติมช่องที่เป็นงานของคุณ  /  เฉลย: starter/shooter_step1.py
# ใบ้: ในใบงาน session (คาบ 15)
# ------------------------------------------------------------------------------
# shooter_step1.py — สร้าง Shooter #1 (คาบ 15): ยานบนสนาม + HUD (ยังไม่ขยับ)
# ------------------------------------------------------------------------------
# เรื่องราวของ step นี้: เราแค่ "วางตัวละครลงสนาม" ก่อน คือยานอวกาศ 1 ลำ
# กับป้ายคะแนนด้านบน. ยังเป็นภาพนิ่ง (ยังไม่อ่านปุ่ม ยังไม่ยิง ยังไม่มีศัตรู)
# พอรันแล้วเห็นยานเขียว ๆ บนจอ ก็ถือว่าก้าวแรกของเราสำเร็จแล้ว
# Core 70% ที่ให้มาแล้ว เรียกใช้ได้เลย: game.start(), game.Box(), game.Text(), game.run()
# C step กับ Python step ให้ผลบนจอเหมือนกัน: ยานลำเดียวกลางล่าง + ข้อความ Score/Lives ด้านบน
#   (เทียบ shooter_step1.c — create() วางยาน + bp_text เหมือนกันเป๊ะ)
# อ้างอิงเกมจริง: reference/shooter_full.py:12-17 ; page_game_shooter.c:593-595 (วางยาน)
# ------------------------------------------------------------------------------
import bentogame as game

game.start()                                  # ล้างจอ + ปลุกจอย (เรียกครั้งเดียว)

# ตัวแปรพื้นฐานของเกม (เก็บไว้ให้โค้ดข้างล่างไม่พัง — ห้ามลบ)
ship = None                                    # ยานของผู้เล่น (เดี๋ยวคุณสร้างเอง)
ship_x = 365.0                                 # ตำแหน่ง x ของยาน (เก็บเป็น float)
score, lives = 0, 3
hud = None                                     # ป้าย HUD (เดี๋ยวคุณสร้างเอง)

# ============ ----- เติมส่วนนี้เอง (งานของคุณ): วางยานของผู้เล่น + ป้าย HUD ----- ============
# 1) สร้างยานด้วย game.Box(...) — เป็นสี่เหลี่ยมแนวนอน (กว้างกว่าสูง)
#    วางกลางจอตามแนวนอน ใกล้ขอบล่าง ใช้สี game.GREEN — แล้วเก็บผลใส่ตัวแปร ship
# 2) สร้างป้าย HUD ด้วย game.Text(...) — ข้อความ "Score: 0   Lives: 3"
#    วางมุมบนซ้าย สี game.WHITE — แล้วเก็บผลใส่ตัวแปร hud
# (ลองกะค่าพิกัด/ขนาดเองก่อนนะ ถ้าติดค่อยเปิดดู starter/shooter_step1.py ; API: bentogame.py Box:178 Text:227)
pass   # <- ลบ pass ออกเมื่อเริ่มเขียน (วางไว้ให้รันผ่านก่อน)
# -----------------------------------------------------------------------------

# ภาพนิ่ง — คาบหน้าทำให้ยานขยับ. on_frame() ที่นี่ยังไม่ขยับอะไร.
# ปุ่มออก/เริ่มใหม่ เอนจิน game.run() จัดการให้: Back = ออก · Start = เริ่มใหม่.
# (ขั้นนี้ยังไม่มี 30% ในลูปเฟรม — แค่รันดูผลงานวาด)
def on_frame():
    pass                                      # ยังไม่มี logic — ภาพนิ่งก่อน

game.run(on_frame, fps=30)                    # Back = ออก / Start = เริ่มใหม่ (เอนจินจัดการเอง)
