# โครงเว้น 30% — มีบางช่องเว้นไว้ให้เราเติมเอง (มองหาคำว่า "เติม:")
#
# s04_thai.py  (คาบ 4 · ของแถม: ภาษาไทยบนจอเกม)
# โชว์ข้อความภาษาไทยบนจอจริง — เฟิร์มแวร์ v1.2.0 วาดอักษรไทยให้เอง
# รวมถึงสระซ้อน/วรรณยุกต์ (เช่น ปั๊มน้ำ เกี๊ยวกุ้ง ผู้รู้ เปรี้ยว)
#
# สองอย่างที่ได้จากไฟล์นี้:
#   1) game.Text("ข้อความไทย", x, y, color)        -> ป้ายข้อความไทย (ขนาดมาตรฐาน)
#   2) ui.Label("ข้อความไทย", x=, y=, color=, value=ขนาด)  -> เลือก "ขนาดตัวอักษร" ได้
#      (game.Text ไม่มีพารามิเตอร์ขนาด — อยากเปลี่ยนขนาดให้ใช้ ui.Label)
#
# วิธีรัน: เปิดไฟล์นี้ใน BENTO IDE แล้วกดปุ่ม Program to Device
# กด Back บนจอยเพื่อออกกลับ Playground · กด Start เพื่อเริ่มใหม่

import bentogame as game
import ui                                 # widget ละเอียด: ui.Label เลือกขนาดตัวอักษรได้

game.title("เกมภาษาไทย")                  # หน้าเริ่ม (Start=เล่น Back=ออก) + เคลียร์จอ — ชื่อเกมเป็นไทยได้เลย

# ---- ป้ายข้อความไทยด้วย game.Text (ขนาดมาตรฐาน) ----
title = game.Text("เกมเก็บของ", 250, 40, game.CYAN)     # ชื่อเกม กลาง ๆ บน
score = game.Text("คะแนน: 0", 20, 20, game.WHITE)       # ป้ายคะแนน มุมซ้ายบน
hint  = game.Text("กดจอยซ้าย-ขวา รับของให้ทัน", 230, 360, game.GB_LIGHTEST)  # คำแนะนำ ล่างจอ

# ---- เลือก "ขนาดตัวอักษร" ด้วย ui.Label(value=ขนาด) ----
# value ที่บอร์ดรองรับ: 14 / 16 / 20 / 24 / 28 (เลขยิ่งมาก ตัวยิ่งใหญ่)
ui.Label("เล็ก 14",  x=120, y=140, color=game.BLUE,   value=14)
ui.Label("กลาง 20",  x=120, y=180, color=game.BLUE,   value=20)
ui.Label("ใหญ่ 28",  x=120, y=230, color=game.YELLOW, value=28)

# ---- คำยาก: สระซ้อน/วรรณยุกต์ เฟิร์มแวร์เรียงให้เองทั้งหมด ----
ui.Label("ปั๊มน้ำ",   x=460, y=150, color=game.PINK,  value=24)
ui.Label("เกี๊ยวกุ้ง", x=460, y=200, color=game.GREEN, value=24)

# ============ ----- เติมส่วนนี้เอง (งานของเรา) ----- ============
# เติม 1: เพิ่มป้ายไทยอีกบรรทัด เป็นคำยากของเราเอง (ลองคำที่มีสระซ้อน เช่น "ผู้รู้" หรือ "เปรี้ยว")
#         ใช้ ui.Label("...", x=460, y=250, color=game.ORANGE, value=24)
pass   # <- ลบ pass ออกเมื่อเริ่มเขียน

points = 0


def update():
    global points
    pressed = game.keys()                 # อ่านจอยเฟรมนี้

    if pressed.a:                         # กด A = บวกแต้ม (ไว้ทดสอบว่าป้ายไทยอัปเดตได้)
        points += 1
        # เติม 2: อัปเดตป้ายคะแนนให้เป็นเลขล่าสุด ด้วย score.set("คะแนน: %d" % points)
        #         (ใช้ป้ายตัวเดิม ไม่สร้างใหม่ทุกเฟรม)
        pass   # <- ลบ pass ออกเมื่อเริ่มเขียน

    # Back = ออก · Start = เริ่มใหม่ — game.run() จัดการให้ก่อนถึง update()


game.run(update)                          # วิ่งทุกเฟรมจนกว่าจะ return False
